package com.secondmonday.hodith.ui.common

import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp

/**
 * Bottom clearance for a scrollable list under a bottom-end FloatingActionButton, so the list's
 * last row isn't covered by it (56dp FAB plus Scaffold's default edge/FAB margins).
 */
internal val FabListBottomClearance: Dp = 88.dp
