<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only this subfolder of the cache dir is exposed via FileProvider, not the whole cache root. -->
    <cache-path name="shared_cards" path="shared_cards/" />
</paths>
