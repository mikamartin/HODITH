# HODITH — Cleanup Log

A record of the 5 most recent cleanup passes, newest first (ordering, not dating, marks recency — see CLAUDE.md's no-dates rule). After any significant feature work, actually walk through every applicable item in [CLEANUP_CHECKLIST.md](CLEANUP_CHECKLIST.md) against the real diff first — an entry here records what that walk-through found, it isn't a template to fill in from memory of what changed. Then add a new entry above the previous one: what was found and fixed, what was deferred with a reason, and which sections didn't apply and why.

**Retention:** only the 5 newest entries are kept here. When adding one pushes the count past 5, delete the oldest entry in the same commit — its content still lives in git history and is reachable via `git log -- docs/CLEANUP_LOG.md`.

## Entry format

```
## <branch or feature name>

**Scope:** what work triggered this pass
**Found & fixed:** bullet list (or "nothing found" — that's a valid result)
**Deferred:** bullet list with reasons (or "nothing deferred")
**Docs updated:** SPEC / TESTING / PLAYBOOK sections touched, if any
```

---

## feat/bulk-delete-logs-by-date

**Scope:** PROGRESS.md's S14 (delete logs older than a chosen date), but built as a rework of the existing "Delete all data" row rather than S14's originally-scoped second row next to it — the user asked for one "Delete Data" action that first chooses all-data vs. logs-only, then (for logs-only) a cutoff date defaulting to today, rather than two separate destructive rows. Cascade scope follows S14's own resolved analysis: Events-only — a resolved Hunch's verdict is a stored snapshot (`fix/freeze-resolved-hunch-verdict`, above), not recomputed live, so trimming old Events can't disturb it or orphan a Hunch/Trigger.

**Changes:**

- **`EventDao`** gained `deleteOlderThan(cutoff)` (`DELETE FROM events WHERE occurredAt < :cutoff`, cascading `event_tags` via the existing FK, `tags` untouched — same shape as `deleteAllData()`) and `getCaseIdsWithEventsOlderThan(cutoff)` (distinct affected Case ids).
- **`HodithRepository.deleteEventsOlderThan(cutoff)`** — new, alongside `deleteAllData()`. `RoomHodithRepository`'s implementation re-evaluates triggers/check-ins for every affected Case afterward (mirroring `deleteEvent`/`deleteEventById`'s existing pattern) since a bulk date-cutoff delete can change a Case's latest-event state the same way a single delete can.
- **`SettingsViewModel`** gained an injected `Clock` (matching `CaseDetailViewModel`'s pattern), `nowMillis()`, and `deleteEventsOlderThan(cutoff)`.
- **New `ui/settings/DeleteDataDialogs.kt`**: `DeleteDataFlow` (the flow's sole entry point, owning its own mode/date/step state), `DeleteDataOptionsDialog` (the all-data/logs-only choice via the existing `SegmentedChoiceRow`, plus a logs-only date button), and `DeleteDataCutoffDatePickerDialog` (mirrors `HunchCreationSheet`'s `WindowStartDatePickerDialog` structure with `LogDetailSheet`'s ceiling-predicate cap flipped in for "no future date" instead of a floor). All three are plain `AlertDialog`/`DatePickerDialog` compositions, the same idiom `ConfirmDialog`/`InfoDialog` already use — no shared dialog shape exists for "arbitrary content plus a confirm/cancel pair," so this stays local to Settings rather than promoted to `ui/common`.
- **`SettingsScreen.kt`**: the old `settingsDeleteAllDataButton` row/dialog wiring replaced with one `showDeleteDataFlow` boolean and a single `DeleteDataFlow(...)` call.
- **Voice ×3**: `settingsDeleteAllDataButton` renamed to `settingsDeleteDataButton`; new `settingsDeleteDataOptionsTitle/OptionAll/OptionLogsOnly/DateLabel/OptionsNextAction/OptionsCancelAction/LogsConfirmTitle/LogsConfirmBody(dateLabel)/LogsConfirmAction`, all three voices given real distinct copy (not shared interface defaults) — the existing `settingsDeleteAllDataConfirmTitle/Body/Action/CancelAction` keys are untouched and still serve the all-data branch.

**Checklist walk (against the working-tree `git diff`):**

- *Duplication* — no composable/styling repeated; every new string went through `Voice` in the same commit, all three voices distinct (`VoiceTest`'s reflection-based "no identical string across voices" check passed, after catching and fixing one accidental Intense/Bright collision on `settingsDeleteDataOptionAll`). `deleteEventsOlderThan` doesn't overlap `deleteAllData` — different scope (Events-only vs. everything including Cases/Tags), not a case for parameterizing into one method.
- *Decoupling* — no composable owns delete logic beyond calling the passed-in callbacks; `SettingsViewModel.nowMillis()` is the only source of "now," backed by the injected `Clock`, no direct `System.currentTimeMillis()`; no `android.*` import anywhere touched (nothing in `domain/`); `SettingsScreen` still takes plain lambdas, not the ViewModel itself.
- *Complexity & pattern health* — first pass left `SettingsScreen`'s main composable at 191 lines (two inline dialog blocks with mode-branching copy); caught on the walk and refactored by extracting the whole delete-data flow — state included — into `DeleteDataFlow` in the new file, bringing it back to 139. `DeleteDataCutoffDatePickerDialog` is single-caller but matches the established precedent of `WindowStartDatePickerDialog`/`LogDetailDatePickerDialog`, both also single-caller helpers local to their screen.
- *Dead code & hygiene* — no unused imports (`ktlintCheck` clean, twice — once after `ktlintFormat` fixed import ordering and wrapped-argument style automatically). No leftover prototype code.
- *Repo hygiene* — no secrets, no local paths; only the intended files touched.
- *Naming* — `DeleteDataDialogs.kt` sits in `ui/settings/` without a `*Screen.kt` suffix, matching the existing precedent of screen-support files like `HunchCreationSheet.kt`. New Voice keys follow the existing `settingsX...` naming run. `DeleteDataMode`/`DeleteDataOptionsDialog`/`DeleteDataCutoffDatePickerDialog` are `private` to the new file — only `DeleteDataFlow` needs `internal` visibility for `SettingsScreen.kt` to call it.
- *Hardcoded values* — none; no new magic numbers or literal colors.
- *Accessibility* — no new icon-only targets; all new interactive elements are standard M3 `TextButton`/`OutlinedButton`/`SegmentedButton` components at their default touch target size.
- *Deprecated APIs* — none introduced; `lintDebug` clean.
- *Spec review* — `HODITH_SPEC.md` §14's Settings row updated to describe the unified action in present tense (no "used to"/"previously" language).
- *Tests* — data-destructive feature, so coverage got a dedicated second pass once an emulator became available, on top of the first pass:
  - `EventDaoTest` (6 new cases): strict-before-cutoff boundary; `event_tags` cascade scoped to deleted Events only; an orphaned tag (used only by a deleted Event) surviving in `tags` untouched — the split `deleteOlderThan`/`tags` invariant wasn't actually exercised by the shared-tag case alone; `deleteOlderThan` reaching every Case's Events, not just one (it's a global bulk action, not Case-scoped — nothing previously pinned that); a no-op on an empty table; distinct affected-Case-id lookup.
  - **New `FakeHodithRepositoryTest` case** for `deleteEventsOlderThan` — this repository's own filter/cascade logic (`events.filter { occurredAt >= cutoff }` + the matching `eventTags` filter) is a second, parallel reimplementation of the DAO's contract (same reasoning `RoomHodithRepositoryLogEventsTest`'s doc comment gives for testing each layer separately), and had no dedicated case at all before this pass — only exercised incidentally, untagged, by `SettingsViewModelTest`'s one case. Covers multi-Case scope, tag cascade, and Cases/Tags left alone in one case, mirroring `deleteCase`'s existing test shape in the same file.
  - `SettingsViewModelTest` (1 new case, first pass): `deleteEventsOlderThan` clears only older Events, keeps Cases/Tags — delegation-only now that `FakeHodithRepositoryTest` covers the deep logic.
  - `SettingsScreenTest` (the two prior delete-all tests reworked for the two-step flow, plus 5 new cases): cancel at the options step, cancel at the confirm step, the logs-only date button defaulting to today, the confirm step invoking `onDeleteEventsOlderThan` with the chosen cutoff, and — added on the coverage re-check — reopening the flow after a cancel resets back to all-data mode rather than resuming the last selection (pins the `DeleteDataFlow` early-return-on-`!visible` reset behaviour its own KDoc claims).
  - Not covered, consistent with the rest of the codebase: actually tapping a different day in the M3 `DatePicker` calendar grid isn't driven by an instrumented test — no date picker anywhere in this app has one (`WindowStartDatePickerDialog`, `LogDetailDatePickerDialog` included); noted in `TESTING.md`'s Deferrals.
  - **Considered and deferred, with sign-off:** `RoomHodithRepository.deleteEventsOlderThan` fetches affected Case ids *before* deleting so it can re-evaluate their triggers/check-ins after — a real ordering bug (querying after delete) would silently return zero affected Cases. No test pins this ordering. Raised with the user rather than decided unilaterally; asked to build the test now vs. accept the gap, the call was neither — write up the gap and check for related ones instead. That surfaced a wider pre-existing gap: `RoomHodithRepository`'s notification-eval side effect is untested at *every* call site (`insertEvent`/`updateEvent`/`deleteEvent`/`deleteEventById` too), already worked around rather than tested in `RoomHodithRepositoryBackupTest.kt` (its own comment: inserts via the raw DAO specifically to avoid triggering the wrapper's fire-and-forget scheduling). Written up as `PROGRESS.md` **D4**, low priority since every affected path self-heals within `NotificationEvalWorker`'s ~6h periodic sweep regardless.
  - `ktlintCheck → lintDebug → testDebugUnitTest (scoped to `SettingsViewModelTest`/`VoiceTest`/`FakeHodithRepositoryTest`) → assembleDebug` all green. `connectedDebugAndroidTest` run scoped to `EventDaoTest` (33/33) and `SettingsScreenTest` (27/27) on `Pixel_8_API36(AVD)`, all clean **after** a real bug the first run caught (see below).
  - **Correction, from the instrumented run:** the first pass gave `settingsDeleteDataButton` (the row label) and `settingsDeleteDataOptionsTitle` (the new dialog's title) the identical string in all three voices ("Delete data", "Erase data", "Nuke data") — caught immediately by `deleteData_defaultsToAllData_confirmInvokesOnDeleteAllData` failing with two matching nodes, because Compose's `AlertDialog` doesn't remove the screen behind it from the semantics tree, so the row and the dialog title were both "Delete data" at once. Not just a test artifact — identical accessible text for two different simultaneously-visible elements is a real accessibility bug too (ambiguous for a screen reader, not only for `onNodeWithText`). Fixed with distinct dialog-title copy per voice ("What to delete" / "What to erase" / "What's getting nuked?"), re-verified no other new string in any voice collides with anything else visible in the same dialog or with the persistent row label.

**Deferred:** the notification-eval scheduling test-coverage gap above (`deleteEventsOlderThan`'s ordering, plus the wider existing gap it surfaced) — written up as `PROGRESS.md` **D4** on explicit sign-off rather than built now. Everything else in S14's acceptance criteria is met, so the item itself is struck from PROGRESS.md rather than reworded.

**Docs updated:** `PROGRESS.md` — S14 struck in full; new **D4** added (notification-eval scheduling coverage gap, deferred). `HODITH_SPEC.md` §14 — Settings row's Data-card clause updated for the unified action. `TESTING.md` — Room DAOs row (`deleteOlderThan`/`getCaseIdsWithEventsOlderThan`), Compose UI row (Delete Data flow coverage replacing the old delete-all clause), Deferrals (the cutoff date-picker calendar-tap gap).

**Verified:** `ktlintCheck → lintDebug → testDebugUnitTest (scoped) → assembleDebug` sequential, all green. `connectedDebugAndroidTest` scoped to `EventDaoTest` (33/33) and `SettingsScreenTest` (27/27) — all green on `Pixel_8_API36(AVD)`.

---

## fix/freeze-resolved-hunch-verdict

**Scope:** Surfaced while scoping PROGRESS.md's S14 (delete logs older than a chosen date) — its open cascade-behavior question ("does deleting old Events also need to handle Hunches/Verdicts?") led to reading `HunchTabState.kt`'s `toHistoryEntry`, which reconstructs a resolved Hunch's historical verdict by recomputing `computeVerdict()` against whatever Events currently exist, filtered to `occurredAt <= resolvedAt`. Its own comment claimed this keeps history "frozen" — true against *new* Events arriving later, but not against edits or deletions of *old* Events inside that window, which silently changed or dropped a closed Hunch's verdict. That gap already existed for any edit to an old Event; S14's bulk date-range delete would have exposed it constantly. Discussed with the user: resolved Hunches should stay exactly as they were at the moment they closed — fixed by persisting the verdict at resolution time instead of recomputing it live. S14 itself stays a separate, not-yet-started item; this branch is only the prerequisite fix.

**Changes:**

- `HunchEntity` gained nullable `resolved*` columns mirroring `VerdictResult` (tier, eventCount, activeDayCount, windowDays, observedRate, expectedRate, comparisonBand) plus `resolvedVerdictSnapshotTaken: Boolean` — the latter needed because a null `comparisonBand` is also the legitimate result for a Hunch resolved before it ever reached a verdict, so it's the only way to tell "genuinely no verdict" apart from "not yet snapshotted." `HodithDatabase` bumped to v10 via a pure-additive auto-migration (same shape as the existing v7→v8).
- `CaseDetailViewModel.resolveHunch` now fetches the Case/Events directly from the repository (not `uiState.value`, which only reflects live data while something is actively collecting it) and persists the computed verdict snapshot alongside `resolvedAt`.
- `HunchTabState.toHistoryEntry` reads the persisted snapshot when present, falling back to the old live-recompute path only as a defensive measure for a row not yet backfilled.
- A schema migration can't run `computeVerdict` (Kotlin domain logic), so it can't backfill pre-existing resolved Hunches — `HodithRepository.backfillResolvedHunchVerdicts()` does that as a one-time-per-row sweep, called from `HodithApplication.onCreate` on every launch (a no-op once every row is snapshotted), freezing each pre-existing resolved Hunch using whatever Events survive at upgrade time.
- `domain/Verdict.kt` gained two small extension functions, `HunchEntity.withResolvedVerdictSnapshot(result)` and `HunchEntity.resolvedVerdictSnapshotOrNull()`, found during the checklist walk below.

**Checklist walk (against the working-tree `git diff`):**

- *Duplication* — real finding: the `hunch.copy(resolvedTier = ..., resolvedComparisonBand = ..., resolvedVerdictSnapshotTaken = true)` seven-field mapping was written three times (`CaseDetailViewModel.resolveHunch`, `RoomHodithRepository.backfillResolvedHunchVerdicts`, `FakeHodithRepository`'s mirror), and the reverse (Hunch → `VerdictResult`) once in `HunchTabState.toHistoryEntry`. Extracted to `withResolvedVerdictSnapshot`/`resolvedVerdictSnapshotOrNull` in `domain/Verdict.kt`, all four call sites updated.
- *Decoupling* — `domain/Verdict.kt` importing `data.HunchEntity` matches the existing precedent (`VerdictEngine.kt` already takes `HunchEntity`/`EventEntity`/`DurationMode` directly); no `android.*` import introduced; `resolveHunch`'s `now` still comes from the injected `Clock`, never `System.currentTimeMillis()`; the backfill uses each Hunch's own already-stored `resolvedAt` as `now`, not the live clock, matching the frozen-history intent.
- *Complexity & pattern health* — n/a, no composables touched.
- *Dead code & hygiene* — no unused imports (`ktlintCheck` clean); no leftover prototype code.
- *Repo hygiene* — `git status` clean of stray files; only the new test file and the generated `app/schemas/.../10.json` are untracked, both intended; no secrets, no local paths.
- *Naming* — new DAO/repository/domain functions follow existing `*Dao`/`observe*`/`get*` and extension-function conventions.
- *Hardcoded values / accessibility / deprecated APIs* — n/a.
- *Spec review* — `HODITH_SPEC.md`'s "Verdicts are computed, never stored" line (§Hunch) was no longer fully accurate for a *resolved* Hunch now that its result is cached on the row; amended with one sentence noting that exception and why (frozen history, not drift).
- *Tests* — `SchemaMigrationCoverageTest` needs no manual bump (it derives its expected count from `schemas/*.json` on disk plus `AUTO_MIGRATION_COUNT`, both already correct). New coverage: a v9→v10 Room migration test (`DatabaseFreshInstallTest`) proving a pre-existing resolved Hunch survives with the new columns unset; `CaseDetailViewModelTest` for `resolveHunch` persisting the snapshot, and a second-pass addition for its new `case == null` early return (a real, if rare, race — the Case deleted out from under an in-flight resolve — not an unreachable guard, so it earned a test rather than being left as untested defensive code); `HunchTabStateTest` for reading the snapshot (ignoring live Events entirely, including a burst that would otherwise flip the result), the null-comparisonBand-omits-from-history case, and the existing "frozen at resolvedAt" tests continuing to exercise the pre-backfill fallback path unchanged (they don't set `resolvedVerdictSnapshotTaken`); `FakeHodithRepositoryTest` and a new Room-backed `RoomHodithRepositoryHunchBackfillTest` for the backfill itself, including idempotency on a second run and leaving an active Hunch alone. Not covered, deliberately: `resolvedVerdictSnapshotOrNull()`'s per-field `?: return null` guards for a row with `resolvedVerdictSnapshotTaken = true` but a partially-null snapshot — both writers (`resolveHunch`, the backfill) always set every field together, so that state is only reachable via a hand-edited or corrupted backup import, not through any code path in the app.
+  - **Audit pass, second look:** re-checked at the user's request — confirmed no `System.currentTimeMillis()` or new `android.*` import anywhere in the diff (grepped, not just asserted), confirmed `androidTest` sources actually compile (`compileDebugAndroidTestKotlin`, not just inferred from `lintDebug`'s lint-model generation), and found the `resolveHunch` case-missing gap above, which is now closed.
  - **Correction, third look:** the "no device available" claim below was wrong — asked directly whether UI tests had run on an emulator, checked `adb devices` rather than re-asserting, and found `emulator-5554` already running. Ran the new instrumented tests for real; see **Verified** below.

**Deferred:** nothing — this branch's own scope (freeze resolved-Hunch verdicts) is fully done; S14 (the feature that motivated finding this gap) is intentionally out of scope here and stays open in `PROGRESS.md`.

**Docs updated:** `HODITH_SPEC.md` — the "computed, never stored" line amended for the resolved-Hunch exception. `TESTING.md` — Verdict engine row (snapshot read/omit/fallback + backfill coverage), Room migrations row (v9→v10). `PROGRESS.md` — S14's cascade-behavior design question marked resolved (Events-only, safe now that resolved-Hunch history no longer recomputes live); this branch's own work isn't listed as a PROGRESS.md item since it's complete, not outstanding.

**Verified:** `ktlintCheck → lintDebug → test → assembleDebug` sequential, all green (run several times across this pass). `connectedDebugAndroidTest` on `Pixel_8_API36(AVD)`: `DatabaseFreshInstallTest` 5/5 (including the new v9→v10 case) and `RoomHodithRepositoryHunchBackfillTest` 4/4, both new; the full `data` package 87/87 as a regression check on `HunchEntity`/`Converters`/`HodithDatabase`/`RoomHodithRepository`; `CaseDetailScreenTest` 33/33; `CaseDetailInsightsTabTest` hit one failure on its first full-class run (`intensityCard_showsAverageIntensityToOneDecimal`, unrelated to this branch — no Hunch/verdict involvement) matching the `ActivityScenario` teardown-timeout flake this file has logged before; an isolated rerun of just that test passed clean, and a subsequent full-class rerun came back 38/38 — confirmed flake, not a regression.

---

## feat/rhythm-card-interaction

**Scope:** PROGRESS.md's S8 — the Rhythm card's legend/grid gap felt cramped, its cells felt small, and unlike `TagsCard`'s tag rows / `IntensityCard`'s squares, its cells weren't a drill-down tap target at all. The open design call (what tapping a cell should open) was confirmed with the user before implementation: the matching logged events for that day-of-week/time-of-day bucket, same shared `InsightsDrillDownDialog` surface as the other three drill-downs.

**Changes:**

- `RhythmCard` (`InsightsTab.kt`): new `RHYTHM_GRID_GAP` constant, independent of `RHYTHM_LABEL_WIDTH`, inserted as its own `Spacer` between the label column and the grid in both the header and each time-of-day row; `RHYTHM_CELL_SIZE` bumped 20 → 26. The former local `timeOfDayLabel` lambda is now the top-level `rhythmTimeOfDayLabel`, shared with `InsightsTabContent`'s new dialog title. Each grid cell is now a `tappableWithDescription` target gated on `cell.count > 0`.
- `RhythmCellDisplay` (`InsightsTabState.kt`) regained a `count: Int` field — it previously dropped the raw count after deriving `HeatmapLevel`, so there was nothing to gate a tap on. Restored from `RhythmStats.cells` in `statsSections()`. Two Preview/test construction sites (`ShareCardTemplate.kt`, `ShareCardTemplateTest.kt`) updated with an explicit `count = 0` — the share-card mini-rhythm never renders real per-cell counts.
- `InsightsTabContent`: new `selectedRhythmCell` state + `onRhythmCellTap` callback threaded through `StatsSectionCards` exactly like `onTagTap`/`onIntensityTap`, opening `InsightsDrillDownDialog` filtered by the tapped day-of-week/time-of-day bucket (reusing the domain's own internal `timeOfDayFor`, so the filter matches `computeRhythmStats`' bucketing exactly).
- Two new `Voice` keys, `insightsRhythmCellTapDescription`/`insightsRhythmDrillDownTitle`, authored in all three voices.
- On-device follow-up: the time-of-day row labels (Morning/Afternoon/Evening/Night) read smaller than sibling cards' `StatRow` text, per user testing — bumped `labelSmall` → `bodyMedium` to match. That font swap alone then ate into `RHYTHM_LABEL_WIDTH`'s margin (truncating "Afternoon"), so the constant went 88 → 112dp, matching the ~1.27× size ratio between the two type scales. Day-of-week header letters (`M`/`T`/…) stay `labelSmall` — untouched, not part of the ask.
- Rhythm gained the same info-icon affordance Frequency/Gaps/Trend/Duration already have (`SectionWithInfo`, replacing the bare section-label `Text`), naming the four `TimeOfDay` boundaries (`MORNING_START_HOUR`/`AFTERNOON_START_HOUR`/`EVENING_START_HOUR`/`NIGHT_START_HOUR`, `domain/StatsEngine.kt`) via `formatClockTime`, so the times shown honour the viewer's own `LocalTimeFormat` (12h/24h) rather than being hardcoded. `insightsRhythmInfoTitle` is a new per-voice `Voice` key (×3); `insightsRhythmInfoBody` was first authored per voice too (a lead sentence + the four lines) but the user asked to drop the lead sentence, leaving just objective clock times with no voiced wording left — so it moved onto the `Voice` interface itself as a shared default (`fun ... = "..."`, no `override` in any voice), the same structural pattern `insightsTimeOfDayMorning`/`insightsSectionLabelRhythm` already use, rather than three copies of an identical string (which would also fail `VoiceTest`'s no-identical-across-voices check for an abstract key).

**Checklist walk (against the working-tree `git diff`):**

- *Duplication* — no inline strings; the two new Voice keys join the existing `...TapDescription`/`...DrillDownTitle` families. Extracting `rhythmTimeOfDayLabel` to file scope avoided a second copy of the time-of-day `when` that the new dialog title would otherwise have needed.
- *Decoupling* — the drill-down filter lives in the Composable, matching the existing Tag/Intensity/Day drill-downs exactly (not a new pattern); `domain/` stays pure Kotlin — the UI layer calls its existing `internal fun timeOfDayFor`, nothing crossed the other way.
- *Complexity & pattern health* — `RhythmCard` stays well under the ~150-line guideline; `selectedRhythmCell` mirrors `selectedTag`/`selectedIntensity`'s `remember` pattern exactly.
- *Accessibility* — real finding: at 26dp, Rhythm's cells fall short of the 48dp minimum touch target, more so than `HeatmapCell`'s own 7-column cells since the label column eats extra width the heatmap doesn't budget for. First attempt applied `HeatmapCell`'s own `minimumInteractiveComponentSize()` treatment — looked right by inspection (all automated checks passed) but broke on an actual device: `HeatmapCell`'s cells sit in a `weight(1f)` row, so the Row gives them an Exact width the modifier can't exceed; Rhythm's row uses fixed `.size()` cells instead (needed to line up with the label column), and `minimumInteractiveComponentSize()` there reports its own inflated size straight into the row's layout, ballooning every column and breaking the grid — caught by the user on-device, not by any of the automated gates. Reverted; cells stay below the 48dp guideline, same tradeoff `IntensityCard`'s own squares already accept when width is tight, rather than risk another silent layout break chasing it further in this pass.
- *Hardcoded values* — `RHYTHM_CELL_SIZE`/`RHYTHM_GRID_GAP`/`RHYTHM_LABEL_WIDTH` are named constants, consistent with each other; the info body reuses `domain`'s existing `MORNING_START_HOUR`-family constants rather than restating the boundary hours as new magic numbers.
- *Duplication (round 2)* — `formatClockTime`/`LocalTimeFormat.current.is24Hour` is the exact pattern `InsightsDrillDownDialog` already uses for event rows; no second time-formatting path introduced. The info body's four-line boundary list is assembled the same "voice takes pre-formatted params" shape as `insightsHeatmapDayTapDescription(formatMediumDate(...))`.
- *Repo hygiene, naming, deprecated APIs* — no findings; `git status` clean of stray files.
- *Spec review* — `HODITH_SPEC.md` §10's drill-down paragraph named only intensity squares and tag rows; updated to include rhythm cells and their zero-count-inert rule. The new info icon itself isn't spec-level UI affordance, same standing call as Frequency/Gaps/Trend/Duration's own icons (`feat/insights-section-info`) — no further spec change.
- *Tests* — `InsightsTabStateTest` gained a case pinning `RhythmCellDisplay.count` against its actual bucket (day/time discrimination itself — the underlying `computeRhythmStats` bucketing — was already exhaustively covered pre-existing in `StatsEngineTest.kt`, so not re-proven here). `CaseDetailInsightsTabTest` gained five cases: `rhythmCell_tap_opensDialogListingOnlyMatchingEvents`, `rhythmCell_zeroCount_staysInert`, `rhythmCard_infoIcon_opensAndDismissesDefinitions` (using `onFirst()` rather than the sibling tests' `onLast()`, since Rhythm's icon is now the first — not last — `caseSectionInfoDescription` node once Frequency is below its 2-event minimum), and `rhythmCard_infoIcon_body_showsBoundariesInTwelveHourFormatByDefault`/`...TwentyFourHourFormat_whenSelected` (the latter needed `setInsightsTabContent` to grow a `timeFormat` param + `LocalTimeFormat` provider, mirroring `CaseDetailScreenTest`'s existing pattern) — these two assert the actual rendered body text against both `TimeFormat` values, the only real end-to-end proof that `formatClockTime`/`LocalTimeFormat.current.is24Hour` are wired correctly rather than just compiling. The three pre-existing info-icon tests' comments updated to name Rhythm alongside Frequency/Gaps in their ordering rationale, since it now also carries the icon. All 38 `CaseDetailInsightsTabTest` cases and all 5 `ShareCardTemplateTest` cases ran green on `Pixel_8_API36(AVD)` (scoped `connectedDebugAndroidTest`, the two files this branch actually touches in `androidTest`).

**Deferred:** a true 48dp touch target for Rhythm cells — would need a bigger layout change (e.g. weight-based columns matching the header, changing the cells' square aspect) than this pass should risk untested; cells stay tappable at their visual 26dp size, sign-off: accept for now.

**Docs updated:** `HODITH_SPEC.md` §10 — drill-down tap-target list now includes rhythm cells. `TESTING.md` — Case Detail Insights row now names Rhythm alongside the other three info icons. `PROGRESS.md` — S8 removed (fully resolved, every acceptance criterion met).

**Verified:** `ktlintCheck → lintDebug → test → assembleDebug` sequential, all green throughout every round of this pass, including after the `minimumInteractiveComponentSize()` revert, the label-width/font follow-ups, and the info-icon addition (one `lintAnalyzeDebugAndroidTest` run crashed with an internal Kotlin-FIR analyzer exception inside an unrelated, untouched file, `BackupImportIntegrationTest.kt`; a bare retry passed clean, confirming it was a transient analyzer crash rather than anything in this diff). `connectedDebugAndroidTest` scoped to `CaseDetailInsightsTabTest` (38/38) and `ShareCardTemplateTest` (5/5) — both green on `Pixel_8_API36(AVD)`, including all five new Rhythm cases. Two on-device layout issues earlier in this pass (the touch-target-expansion overflow, and the label truncation) were only caught by the user's own device checks, not by any automated gate — a reminder these gates don't cover visual layout; this final device run confirms the current state renders correctly.

---

## feat/insights-section-info

**Scope:** PROGRESS.md's S16 — added the existing per-section info affordance (`SectionWithInfo`/`InfoDialog`, already used by Frequency and Gaps & streaks) to the Trend and Duration cards on the Case Detail Insights tab. Rhythm and Tags were shortlisted and declined: their content is already self-explanatory (a heatmap grid, a per-tag count against the case total) without a dedicated explanation.

**Changes:**

- `TrendCard` and `DurationCard` (`InsightsTab.kt`) now wrap their content in `SectionWithInfo`, adding a tappable info icon next to each section label — same shape as the existing `GapsCard`/`FrequencyCard` usage, no other logic touched.
- Four new `Voice` keys (`insightsTrendInfoTitle`/`Body`, `insightsDurationInfoTitle`/`Body`) added to all three voices. Duration's copy is a static sentence ("still-running events aren't counted until they stop") rather than S16's suggested dynamic "N of M events had a duration" count — declined on sign-off, to avoid extending `DurationStats`/`DurationDisplay` for an edge-case detail. Trend's copy describes the current 30-vs-30-day comparison and gap/streak shift notes directly, rather than waiting on S2 (an unstarted, separate investigation into whether that math itself should change).

**Checklist walk (against the working-tree `git diff`):**

- *Duplication, decoupling, complexity, hardcoded values, accessibility, deprecated APIs, repo hygiene, naming* — no findings; the change reuses the existing shared component and Voice pattern exactly, no new composables and no strings outside Voice.
- *Dead code & hygiene / tests* — found `CaseDetailInsightsTabTest.gapsCard_infoIcon_opensAndDismissesDefinitions`'s comment ("Frequency and Gaps & streaks are the only two cards with an info icon") had gone stale: it still passed only because that test's fixture (short history, `NONE` duration mode) keeps the new Trend/Duration icons hidden, not because they don't exist. Fixed the comment on sign-off, and added `trendCard_infoIcon_opensAndDismissesDefinitions` / `durationCard_infoIcon_opensAndDismissesDefinitions`, mirroring the existing Gaps coverage — closing the analogous coverage gap the new icons introduced.
- *Spec review* — `HODITH_SPEC.md` §10 describes what Trend and Duration compute, not the info-icon UI affordance (true for Frequency/Gaps already too), so no divergence.

**Deferred:** nothing — Rhythm and Tags were considered and declined rather than deferred (see Scope above), and S16 is otherwise complete, so it's removed from PROGRESS.md rather than struck.

**Docs updated:** `TESTING.md`'s Case Detail Insights coverage row now names the Trend and Duration info icons alongside Gaps'; PROGRESS.md's S16 item removed (fully resolved).

---

## feat/hunch-history-row-redesign

**Scope:** PROGRESS.md's S5. The trigger was a bug — `monthsAgo`/`hunchHistoryRowWhen` reads "0 months ago" for anything resolved inside its first month — but the item called for a full design-and-content pass, prototyped via an Artifact mockup before any Compose changes. The design review itself surfaced a second, unrelated bug: `HunchTabState.EarlyDays`/`Verdict` never carried `history` at all, so a Case's entire resolved-Hunch record disappeared from the screen the moment a new Hunch went active.

**Changes:**

- `HunchTabState`'s `history` moved onto the sealed interface itself (all three subtypes already carried it), so it's read unconditionally in `CaseDetailScreen.kt` rather than only inside the `NoActiveHunch` branch.
- `HunchHistoryRow` rebuilt to lead with a `Made … · Resolved …` stamp (`hunchHistoryRowStamp`, new Voice key ×3) using absolute dates via the existing `formatEventDate` — no live-clock dependency left in the row at all. Rows are separated by `HorizontalDivider`s inside a card now sitting on `MaterialTheme.colorScheme.surfaceVariant` (via a new `containerColor` param on the shared `HunchCard` shell) instead of default surface, so a closed record reads as visibly distinct from the live active-Hunch card above it. No verdict-tier badge in the row — stays only on the live verdict card.
- `hunchHistoryRowOutcome` reworded from a binary held-up/off to three severity tiers (`ABOUT_RIGHT` / `LESS`+`MORE` / `MUCH_LESS`+`MUCH_MORE`) — found during the design review, fixed in this same pass on explicit sign-off rather than deferred. `hunchHistorySummary` and `hunchHistoryRowOutcome` also lost their em dashes (×3 voices each), on request.
- `monthsAgo` (`CaseDetailViewModel.kt`) and `hunchHistoryRowWhen` (×3, `Voice.kt`) deleted outright.

**Checklist walk (against the working-tree `git diff`):**

- *Duplication* — first pass had `HunchHistoryCard` hand-roll its own `Card` + padded `Column` to get a different tone, duplicating `HunchCard`'s existing shell; caught and folded back in via the new `containerColor` param (default unchanged, so every other Hunch-tab card call site is untouched). No inline strings — everything through Voice; `hunchHistoryRowStamp` is the only new key, added to all three voices in this commit.
- *Decoupling* — `HunchHistoryRow` calls `formatEventDate` directly, same convention `formatEventTime`/`formatRate`/`formatExpectedFrequency` already use in this file. No `System.currentTimeMillis()`; `now` is no longer threaded into the history composables at all now that both dates are absolute. No `android.*` in domain code (untouched).
- *Complexity & pattern health* — first pass computed `history` via a value-returning `when` with side-effecting composable calls inside each branch; reworked by lifting `history` onto the interface so `HunchTabContent` goes back to a plain dispatch `when`, `state.history` read once afterward.
- *Dead code & hygiene* — grepped every `monthsAgo`/`hunchHistoryRowWhen` reference; also caught a dangling KDoc mention of `[monthsAgo]` in `TriggersViewModel.kt`'s `triggerRows` doc comment (reworded), and my own new doc-comments citing "(spec S5)" as if it were a stable `HODITH_SPEC.md` section rather than a PROGRESS.md tracker code this same diff struck out — removed from all six spots before they could go stale on merge. The Artifact prototype used to pick the layout lived only in the session scratchpad, never the repo (`git status` confirmed clean).
- *Repo hygiene, naming, hardcoded values, accessibility, deprecated APIs* — no findings.
- *Spec review* — `HODITH_SPEC.md` §7's hunch-history paragraph describes resolve → archive → frozen-verdict at an abstract level, not row layout, so it stays accurate; no update needed.
- *Tests* — `HunchTabStateTest` gained two cases pinning `history` on `EarlyDays`/`Verdict` (the exact visibility bug). `CaseDetailScreenTest` gained a regression guard for the visibility bug and a second case locking the new stamp field's exact rendered text — closing a first-pass gap where the row's actual headline change had no UI coverage at all. `VoiceTest` gained a case pinning the three-severity-tier fix (LESS/MORE now read distinctly from ABOUT_RIGHT and from MUCH_LESS/MUCH_MORE) — the pure-Voice-logic equivalent of a regression test, at the unit level rather than Compose, since `hunchHistoryRowOutcome` has no Android dependency. `CaseDetailFormattingTest`'s two `monthsAgo` cases removed with the function. `VoiceTest`'s existing reflection walk covers every other new/removed key automatically.

**Deferred:** nothing — the one finding from this pass (outcome-text granularity) was fixed in the same branch on explicit sign-off rather than deferred.

**Docs updated:** `PROGRESS.md` — S5 struck in full (all acceptance criteria met), its two cross-references in the Standalone intro and B2's copy-touching list removed.

**Verified:** `ktlintCheck → lintDebug → test → assembleDebug` sequential, all green. `connectedDebugAndroidTest` scoped to `CaseDetailScreenTest` — 32/32 green on `Pixel_8_API36(AVD)`, including both new cases.

